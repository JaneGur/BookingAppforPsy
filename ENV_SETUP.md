# Настройка переменных окружения

## Ошибка: SUPABASE_SERVICE_ROLE_KEY

Если вы видите ошибку:
```
"Заблокированные дни недоступны: не настроен серверный доступ к базе (SUPABASE_SERVICE_ROLE_KEY)."
```

Это значит, что не настроены переменные окружения для Supabase.

## Инструкция по настройке

### 1. Создайте файл `.env.local` в корне проекта

```bash
# В корне проекта (рядом с package.json)
touch .env.local
```

### 2. Получите ключи из Supabase Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте следующие значения:

   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role secret** → `SUPABASE_SERVICE_ROLE_KEY` ⚠️ **Держите в секрете!**

### 3. Заполните `.env.local`

Скопируйте в `.env.local`:

```env
# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Найдите эти ключи в: Supabase Dashboard → Project Settings → API

# URL вашего Supabase проекта (публичный)
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект-id.supabase.co

# Публичный anon ключ (безопасно использовать на клиенте)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role ключ (ТОЛЬКО для сервера, НЕ публиковать!)
# ⚠️ ВАЖНО: Этот ключ имеет полный доступ к базе данных
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# NEXTAUTH CONFIGURATION
# ============================================
NEXTAUTH_URL=http://localhost:3000

# Сгенерируйте секретный ключ:
# openssl rand -base64 32
NEXTAUTH_SECRET=ваш-секретный-ключ-минимум-32-символа

# ============================================
# TELEGRAM BOT (опционально)
# ============================================
# Получите от @BotFather в Telegram
# TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# ID чата администратора (отправьте /start боту и посмотрите ID)
# TELEGRAM_ADMIN_CHAT_ID=123456789
```

### 4. Перезапустите сервер разработки

```bash
# Остановите текущий процесс (Ctrl+C)
# Запустите заново
npm run dev
```

## Важные примечания

### 🔒 Безопасность

- ❌ **НИКОГДА** не коммитьте `.env.local` в Git
- ❌ **НИКОГДА** не публикуйте `SUPABASE_SERVICE_ROLE_KEY`
- ✅ Файл `.env.local` уже добавлен в `.gitignore`

### 📝 Различия между ключами

| Ключ | Где использовать | Безопасность |
|------|-----------------|--------------|
| `NEXT_PUBLIC_SUPABASE_URL` | Клиент + Сервер | Публичный |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Клиент + Сервер | Публичный (защищен RLS) |
| `SUPABASE_SERVICE_ROLE_KEY` | **ТОЛЬКО Сервер** | ⚠️ Секретный |

### 🔧 Генерация NEXTAUTH_SECRET

```bash
# Используйте один из методов:

# Метод 1: OpenSSL (Linux/Mac)
openssl rand -base64 32

# Метод 2: Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Метод 3: Онлайн генератор
# https://generate-secret.vercel.app/32
```

## Проверка настройки

После настройки `.env.local` проверьте:

```bash
# 1. Перезапустите сервер
npm run dev

# 2. Откройте браузер
# http://localhost:3000

# 3. Попробуйте:
# - Зарегистрироваться
# - Посмотреть доступные слоты
# - Войти в админ панель
```

Если всё настроено правильно, ошибки исчезнут! ✅

## Troubleshooting

### Ошибка всё ещё появляется?

1. **Проверьте имя файла**: должен быть именно `.env.local` (с точкой в начале)
2. **Проверьте путь**: файл должен быть в корне проекта
3. **Проверьте значения**: убедитесь, что скопировали полные ключи
4. **Перезапустите**: обязательно перезапустите `npm run dev`
5. **Проверьте пробелы**: не должно быть пробелов вокруг `=`

### Пример правильного формата:

```env
# ✅ Правильно
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ❌ Неправильно (пробелы)
SUPABASE_SERVICE_ROLE_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ❌ Неправильно (кавычки не нужны)
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

## Production (Vercel/Hosting)

Для production добавьте эти же переменные в настройки вашего хостинга:

### Vercel:
1. Project Settings → Environment Variables
2. Добавьте все переменные из `.env.local`
3. Redeploy

### Другие платформы:
- Railway: Settings → Variables
- Netlify: Site settings → Build & deploy → Environment
- AWS/GCP: Следуйте документации платформы
