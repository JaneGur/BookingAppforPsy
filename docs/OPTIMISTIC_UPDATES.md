# ⚡ Optimistic Updates - Мгновенный UI

## Что это такое?

**Optimistic Updates** — это техника, при которой UI обновляется мгновенно, еще до получения ответа от сервера. Пользователь видит изменения сразу же, что создает впечатление молниеносной работы приложения.

### Как это работает?

```
1. Пользователь нажимает кнопку "Подтвердить оплату"
2. ✅ UI мгновенно обновляется (статус → "confirmed")
3. 📡 Запрос отправляется на сервер
4. ✅ Сервер подтверждает → всё ОК
   ❌ Сервер отклоняет → UI откатывается к старому состоянию
```

## Где используется в проекте

### 1. Изменение статуса записи
**Где:** `/admin/clients/[id]`, `BookingsTab`, `BookingDetailsModal`

```typescript
import { useUpdateBookingStatus } from '@/lib/hooks'

// В компоненте
const updateStatus = useUpdateBookingStatus()

// При клике
await updateStatus.mutateAsync({
  id: booking.id,
  status: 'confirmed'
})

// UI обновится МГНОВЕННО! ⚡
// Нет ожидания ответа сервера
```

**Что видит пользователь:**
- ✅ Клик → статус меняется мгновенно
- ✅ Цвет badge обновляется сразу
- ✅ Никаких лоадеров или задержек

### 2. Редактирование данных клиента
**Где:** `/admin/clients/[id]`

```typescript
import { useUpdateClient } from '@/lib/hooks'

const updateClient = useUpdateClient()

await updateClient.mutateAsync({
  clientId: '123',
  data: { email: 'new@example.com' }
})

// Email обновится в UI мгновенно! ⚡
```

**Что видит пользователь:**
- ✅ Сохранение → данные обновляются мгновенно
- ✅ Форма закрывается без задержек
- ✅ Новые данные видны сразу

### 3. Удаление записей
**Где:** `BookingsTab`, `BookingDetailsModal`

```typescript
import { useDeleteBooking } from '@/lib/hooks'

const deleteBooking = useDeleteBooking()

await deleteBooking.mutateAsync(booking.id)

// Запись исчезает из списка МГНОВЕННО! ⚡
```

**Что видит пользователь:**
- ✅ Удалить → запись исчезает сразу
- ✅ Статистика обновляется мгновенно
- ✅ Плавная анимация исчезновения

### 4. Удаление клиентов
**Где:** `/admin/clients`, `/admin/clients/[id]`

```typescript
import { useDeleteClient } from '@/lib/hooks'

const deleteClient = useDeleteClient()

await deleteClient.mutateAsync(clientId)

// Клиент удаляется из списка МГНОВЕННО! ⚡
```

### 5. Создание новых записей
**Где:** `CreateBookingModal`, публичная форма записи

```typescript
import { useCreateBooking } from '@/lib/hooks'

const createBooking = useCreateBooking()

await createBooking.mutateAsync({
  client_name: 'Иван Иванов',
  booking_date: '2026-02-15',
  // ...
})

// Новая запись появляется в списке МГНОВЕННО! ⚡
```

## Технические детали

### Структура хука с Optimistic Update

```typescript
export function useUpdateBookingStatus() {
    const queryClient = useQueryClient()

    return useMutation({
        mutationFn: async ({ id, status }) => {
            // Реальный запрос на сервер
            const res = await fetch(`/api/bookings/${id}`, {
                method: 'PATCH',
                body: JSON.stringify({ status }),
            })
            return res.json()
        },
        
        // 1. ПЕРЕД запросом - обновляем UI оптимистично
        onMutate: async ({ id, status }) => {
            // Отменяем текущие запросы
            await queryClient.cancelQueries({ queryKey: ['bookings'] })
            
            // Сохраняем старые данные (для отката)
            const previousData = queryClient.getQueryData(['bookings'])
            
            // ⚡ МГНОВЕННО обновляем UI
            queryClient.setQueriesData({ queryKey: ['bookings'] }, (old: any) => {
                return old.map((booking: Booking) =>
                    booking.id === id ? { ...booking, status } : booking
                )
            })
            
            return { previousData } // для rollback
        },
        
        // 2. При ошибке - откатываем UI
        onError: (err, variables, context) => {
            // ❌ Что-то пошло не так - возвращаем старое состояние
            if (context?.previousData) {
                queryClient.setQueryData(['bookings'], context.previousData)
            }
        },
        
        // 3. После завершения - синхронизируем с сервером
        onSettled: () => {
            // ✅ Обновляем данные с сервера (финальная синхронизация)
            queryClient.invalidateQueries({ queryKey: ['bookings'] })
        },
    })
}
```

### Преимущества

1. **Мгновенный отклик** ⚡
   - Пользователь видит изменения сразу
   - Нет ощущения "тормозов"

2. **Надежность** 🛡️
   - При ошибке UI откатывается
   - Данные всегда синхронизированы

3. **Лучший UX** 🎨
   - Приложение кажется в 10x быстрее
   - Пользователь не ждет ответа сервера

4. **Простота использования** 👨‍💻
   - Один хук вместо useState + fetch
   - Автоматическое управление состоянием

### Недостатки и особенности

1. **Временные несоответствия**
   - UI может показывать не совсем актуальные данные (доли секунды)
   - Решается через `onSettled` - финальная синхронизация

2. **Требует правильной структуры**
   - Нужно использовать React Query
   - Данные должны быть в кеше

3. **Обработка ошибок**
   - Важно показывать пользователю toast/alert при откате
   - В текущей реализации - консоль + toast (добавить везде)

## Примеры использования в компонентах

### BookingsTab.tsx

```typescript
'use client'

import { useUpdateBookingStatus, useDeleteBooking } from '@/lib/hooks'

export function BookingsTab() {
    // 🚀 Инициализация хуков
    const updateStatus = useUpdateBookingStatus()
    const deleteBooking = useDeleteBooking()

    // 🎯 Обработчики
    const handleStatusChange = async (id: number, status: Booking['status']) => {
        await updateStatus.mutateAsync({ id, status })
        // UI обновился МГНОВЕННО, запрос идет в фоне
    }

    const handleDelete = async (id: number) => {
        if (!confirm('Удалить?')) return
        await deleteBooking.mutateAsync(id)
        // Запись исчезла МГНОВЕННО
    }

    // ... остальной код
}
```

### /admin/clients/[id]/page.tsx

```typescript
'use client'

import { useUpdateClient, useUpdateBookingStatus } from '@/lib/hooks'

export default function ClientDetailPage() {
    const updateClient = useUpdateClient()
    const updateBookingStatus = useUpdateBookingStatus()

    const handleSave = async () => {
        await updateClient.mutateAsync({
            clientId,
            data: { name, email, telegram }
        })
        // Данные обновились МГНОВЕННО
        setIsEditing(false)
    }

    // ... остальной код
}
```

## React Query configuration

Optimistic updates работают благодаря React Query. Наша конфигурация:

```typescript
// lib/providers/QueryProvider.tsx
const queryClient = new QueryClient({
    defaultOptions: {
        queries: {
            staleTime: 60000, // 1 минута
            refetchOnWindowFocus: false,
        },
    },
})
```

## Debugging

### Включить React Query DevTools

```tsx
// В development mode
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'

<QueryClientProvider client={queryClient}>
    {children}
    <ReactQueryDevtools initialIsOpen={false} />
</QueryClientProvider>
```

Это позволит видеть:
- Все запросы в реальном времени
- Состояние кеша
- Optimistic updates в действии

## Best Practices

### ✅ DO

```typescript
// Используйте optimistic updates для:
- Изменения статусов
- Редактирования простых полей
- Удаления элементов
- Создания новых записей

// Показывайте feedback при ошибках
onError: (error) => {
    toast.error('Не удалось сохранить изменения')
    console.error(error)
}
```

### ❌ DON'T

```typescript
// НЕ используйте для:
- Сложных бизнес-операций с валидацией
- Финансовых транзакций (оплата)
- Операций с побочными эффектами

// НЕ забывайте обрабатывать ошибки
onError: () => {} // ❌ Плохо
```

## Дальнейшие улучшения

### 1. Добавить toast-уведомления везде

```typescript
import { toast } from 'sonner'

onError: (error) => {
    toast.error('Ошибка: ' + error.message)
}

onSuccess: () => {
    toast.success('Изменения сохранены')
}
```

### 2. Добавить оптимистичные анимации

```typescript
// При удалении - плавное исчезновение
<motion.div
    initial={{ opacity: 1 }}
    exit={{ opacity: 0, height: 0 }}
    transition={{ duration: 0.3 }}
>
    {/* Карточка записи */}
</motion.div>
```

### 3. Расширить на другие сущности

- Продукты (create, update, delete)
- Заблокированные слоты (create, delete)
- Настройки (update)

## Заключение

Optimistic updates — это мощная техника, которая делает приложение в несколько раз отзывчивее. Мы внедрили её для всех ключевых операций:

- ✅ Изменение статусов записей
- ✅ Редактирование клиентов
- ✅ Удаление записей и клиентов
- ✅ Создание новых записей

Теперь админ-панель работает молниеносно! ⚡
