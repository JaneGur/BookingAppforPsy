# 📦 Настройка хранилища документов (Supabase Storage)

## Шаг 1: Создание bucket

1. Откройте **Supabase Dashboard**
2. Перейдите в **Storage** (боковое меню)
3. Нажмите **"New bucket"**
4. Заполните форму:
   - **Name:** `documents`
   - **Public bucket:** ✅ **Включите** (чтобы документы были доступны по ссылке)
   - **File size limit:** `10 MB` (опционально)
   - **Allowed MIME types:** `application/pdf, application/msword, application/vnd.openxmlformats-officedocument.wordprocessingml.document` (опционально)
5. Нажмите **"Create bucket"**

## Шаг 2: Настройка политик доступа (RLS)

### Вариант A: Через SQL Editor (рекомендуется)

Откройте **SQL Editor** и выполните:

```sql
-- Создаем bucket (если еще не создан)
INSERT INTO storage.buckets (id, name, public)
VALUES ('documents', 'documents', true)
ON CONFLICT (id) DO NOTHING;

-- Разрешаем всем читать файлы (public bucket)
CREATE POLICY "Public Access"
ON storage.objects FOR SELECT
USING (bucket_id = 'documents');

-- Разрешаем админам загружать файлы
CREATE POLICY "Admin Upload"
ON storage.objects FOR INSERT
WITH CHECK (
    bucket_id = 'documents' AND
    auth.role() = 'authenticated'
);

-- Разрешаем админам удалять файлы
CREATE POLICY "Admin Delete"
ON storage.objects FOR DELETE
USING (
    bucket_id = 'documents' AND
    auth.role() = 'authenticated'
);
```

### Вариант B: Через UI

1. Перейдите в **Storage** → **Policies**
2. Выберите bucket **"documents"**
3. Создайте 3 политики:

#### Политика 1: Public Access (SELECT)
```
Operation: SELECT
Policy name: Public Access
Policy definition:
USING (bucket_id = 'documents')
```

#### Политика 2: Admin Upload (INSERT)
```
Operation: INSERT
Policy name: Admin Upload
WITH CHECK:
bucket_id = 'documents' AND auth.role() = 'authenticated'
```

#### Политика 3: Admin Delete (DELETE)
```
Operation: DELETE
Policy name: Admin Delete
USING:
bucket_id = 'documents' AND auth.role() = 'authenticated'
```

## Шаг 3: Проверка настроек

### Проверка bucket

```sql
-- Проверяем, что bucket создан и публичный
SELECT * FROM storage.buckets WHERE id = 'documents';
```

**Ожидаемый результат:**
| id | name | public |
|----|------|--------|
| documents | documents | true |

### Проверка политик

```sql
-- Проверяем политики
SELECT schemaname, tablename, policyname, permissive, roles, cmd
FROM pg_policies
WHERE tablename = 'objects' AND policyname LIKE '%documents%';
```

## Шаг 4: Тестирование загрузки

1. Откройте админ-панель: `/admin/settings`
2. Перейдите на вкладку **"Документы"**
3. Выберите **"Загрузить файл"**
4. Загрузите тестовый PDF
5. Проверьте, что файл появился в списке

### Если загрузка не работает:

#### Проверка 1: Переменные окружения
Убедитесь, что в `.env.local` есть:
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

#### Проверка 2: Логи
Откройте консоль браузера (F12) и проверьте ошибки при загрузке.

#### Проверка 3: Storage в Supabase
Перейдите в **Storage** → **documents** и проверьте, появились ли файлы.

## Шаг 5: Настройка CORS (если нужно)

Если файлы не отображаются из-за CORS:

1. Перейдите в **Settings** → **API**
2. Найдите **CORS settings**
3. Добавьте ваш домен в **Allowed origins**

---

## 🎯 Готово!

Теперь вы можете:
- ✅ Загружать файлы через админ-панель
- ✅ Документы автоматически сохраняются в Supabase Storage
- ✅ Получать публичные ссылки на файлы
- ✅ Документы доступны клиентам по ссылкам

---

## 📝 Дополнительная информация

### Поддерживаемые форматы
- PDF (`.pdf`)
- Word (`.doc`, `.docx`)

### Ограничения
- Максимальный размер файла: **10 МБ**
- Количество файлов: **неограничено**

### Структура хранения
```
documents/
  ├── offer_1737538800000_document.pdf
  ├── policy_1737538900000_privacy.pdf
  └── ...
```

Формат имени файла: `{type}_{timestamp}_{original_name}`

### Безопасность
- ✅ Файлы доступны только по прямой ссылке (public bucket)
- ✅ Загрузка доступна только администраторам (authenticated)
- ✅ Удаление доступно только администраторам
- ✅ Файлы валидируются по типу и размеру

---

## ⚠️ Важные примечания

1. **Public bucket** означает, что любой, кто знает ссылку, может скачать файл. Это нормально для юридических документов.

2. **Старые файлы не удаляются автоматически**. При добавлении нового документа:
   - Старая запись в БД деактивируется
   - Старый файл остается в Storage
   - Можно настроить автоудаление через Supabase Functions (опционально)

3. **Резервное копирование**: Supabase автоматически создает резервные копии, но рекомендуется периодически скачивать важные документы.

---

## 🆘 Troubleshooting

### Ошибка: "Failed to upload file"
- Проверьте политики Storage
- Убедитесь, что bucket публичный
- Проверьте переменные окружения

### Ошибка: "File too large"
- Максимальный размер: 10 МБ
- Сожмите PDF или используйте оптимизацию

### Ошибка: "Unsupported file type"
- Разрешены только: PDF, DOC, DOCX
- Проверьте расширение файла

### Файлы не отображаются
- Проверьте публичность bucket
- Проверьте CORS настройки
- Откройте ссылку напрямую в браузере

---

**Версия:** 1.0.0  
**Дата:** Январь 2026
