# ✅ Чек-лист: Настройка загрузки документов

## 🔧 1. Настройка Supabase Storage

### Вариант A: Через UI (рекомендуется для начинающих)

1. **Откройте Supabase Dashboard**
   - Перейдите на ваш проект
   - Нажмите на **Storage** в боковом меню

2. **Создайте bucket**
   - Нажмите **"New bucket"**
   - **Name:** `documents`
   - **Public bucket:** ✅ **Включите галочку!**
   - Нажмите **"Create bucket"**

3. **Настройте политики**
   - Перейдите в **Policies** (внутри Storage)
   - Нажмите **"New Policy"** для bucket `documents`
   
   **Создайте 3 политики:**
   
   a) **Public Access (SELECT)**
   ```
   Policy name: Public Access
   Operation: SELECT
   USING: bucket_id = 'documents'
   ```
   
   b) **Admin Upload (INSERT)**
   ```
   Policy name: Admin Upload
   Operation: INSERT
   WITH CHECK: bucket_id = 'documents' AND auth.role() = 'authenticated'
   ```
   
   c) **Admin Delete (DELETE)**
   ```
   Policy name: Admin Delete
   Operation: DELETE
   USING: bucket_id = 'documents' AND auth.role() = 'authenticated'
   ```

### Вариант B: Через SQL Editor (быстрее)

1. **Откройте SQL Editor** в Supabase
2. **Скопируйте и выполните:**

```sql
-- Создаем bucket
INSERT INTO storage.buckets (id, name, public)
VALUES ('documents', 'documents', true)
ON CONFLICT (id) DO NOTHING;

-- Политика: все могут читать
CREATE POLICY "Public Access"
ON storage.objects FOR SELECT
USING (bucket_id = 'documents');

-- Политика: только админы загружают
CREATE POLICY "Admin Upload"
ON storage.objects FOR INSERT
WITH CHECK (
    bucket_id = 'documents' AND
    auth.role() = 'authenticated'
);

-- Политика: только админы удаляют
CREATE POLICY "Admin Delete"
ON storage.objects FOR DELETE
USING (
    bucket_id = 'documents' AND
    auth.role() = 'authenticated'
);
```

3. **Проверьте:**

```sql
-- Должен вернуть 1 строку с public = true
SELECT * FROM storage.buckets WHERE id = 'documents';
```

---

## ✅ 2. Проверка настроек

- [ ] Bucket `documents` создан
- [ ] Bucket публичный (public = true)
- [ ] 3 политики созданы (SELECT, INSERT, DELETE)
- [ ] Переменные окружения настроены:
  ```env
  NEXT_PUBLIC_SUPABASE_URL=...
  NEXT_PUBLIC_SUPABASE_ANON_KEY=...
  SUPABASE_SERVICE_ROLE_KEY=...
  ```

---

## 📤 3. Тестирование загрузки

1. **Откройте админ-панель:** `/admin/settings`
2. **Перейдите на вкладку "Документы"**
3. **Выберите "Загрузить файл"** (должна быть активна по умолчанию)
4. **Заполните форму:**
   - [ ] Тип документа: Оферта
   - [ ] Название: "Тестовый документ"
   - [ ] Файл: выберите любой PDF (до 10 МБ)
5. **Нажмите "Загрузить документ"**
6. **Проверьте результат:**
   - [ ] Появилось уведомление "Документ загружен"
   - [ ] Документ появился в списке ниже
   - [ ] Есть кнопка "Открыть"
   - [ ] По клику на "Открыть" файл открывается

---

## 🔍 4. Проверка в Supabase

1. **Откройте Storage** в Supabase Dashboard
2. **Перейдите в bucket "documents"**
3. **Проверьте:**
   - [ ] Файл присутствует
   - [ ] Имя файла: `offer_TIMESTAMP_filename.pdf`
   - [ ] Файл доступен (можно скачать)

---

## 🎯 5. Тест разных сценариев

### Тест 1: Загрузка PDF
- [ ] Загрузите PDF файл
- [ ] Должно работать ✅

### Тест 2: Загрузка DOCX
- [ ] Загрузите DOCX файл
- [ ] Должно работать ✅

### Тест 3: Большой файл (> 10 МБ)
- [ ] Попробуйте загрузить файл больше 10 МБ
- [ ] Должна появиться ошибка "Файл слишком большой" ❌

### Тест 4: Неподдерживаемый формат
- [ ] Попробуйте загрузить JPG/PNG
- [ ] Должна появиться ошибка "Неподдерживаемый формат" ❌

### Тест 5: Загрузка через URL (старый метод)
- [ ] Переключитесь на "Указать URL"
- [ ] Введите URL документа
- [ ] Должно работать ✅

---

## 🆘 Troubleshooting

### Ошибка: "Failed to upload file"

**Решение:**
1. Проверьте, что bucket создан и публичный
2. Проверьте политики Storage
3. Убедитесь, что вы авторизованы как admin

### Ошибка: "CORS error"

**Решение:**
1. Откройте Supabase → Settings → API
2. Добавьте ваш домен в CORS allowed origins
3. Перезапустите приложение

### Файл загружается, но не открывается

**Решение:**
1. Проверьте, что bucket публичный (public = true)
2. Откройте ссылку напрямую в браузере
3. Проверьте политику SELECT

### Не вижу кнопку "Загрузить файл"

**Решение:**
1. Перезагрузите страницу (Ctrl+F5)
2. Очистите кэш браузера
3. Проверьте консоль на ошибки

---

## 📝 Дополнительная информация

### Поддерживаемые форматы:
- ✅ PDF (`.pdf`)
- ✅ Word (`.doc`, `.docx`)

### Ограничения:
- 📦 Максимальный размер: **10 МБ**
- 🔢 Количество файлов: **неограничено**

### Где хранятся файлы:
```
Supabase Storage
└── documents/
    ├── offer_1737538800000_document.pdf
    ├── policy_1737538900000_privacy.pdf
    └── ...
```

### Безопасность:
- ✅ Файлы публичны (доступны по ссылке)
- ✅ Загрузка только для админов
- ✅ Удаление только для админов
- ✅ Валидация типа и размера

---

## ✅ Готово!

После выполнения всех шагов вы можете:
- 📤 Загружать документы с компьютера
- 🔗 Или добавлять по URL
- 📄 Документы автоматически доступны клиентам

---

## 📚 Полная документация

- **Настройка Storage:** `docs/SETUP_DOCUMENTS_STORAGE.md`
- **Резюме функционала:** `DOCUMENT_UPLOAD_SUMMARY.md`
- **SQL скрипт:** `supabase/create-documents-bucket.sql`

---

**Удачи! 🚀**
